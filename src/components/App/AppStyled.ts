import styled from "styled-components";

const AppStyled = styled.div`
  margin: 0 auto;

  .app-container {
    padding: 25px;
  }

  .title {
    padding-bottom: 30px;
    text-align: center;
    font-size: 2rem;
  }
`;

export default AppStyled;
